{
  "version": 2,
  "name": "your-app-name",
  "builds": [{ "src": "package.json", "use": "@vercel/node" }],
  "routes": [{ "src": "/(.*)", "dest": "/" }],
  "build": {
    "env": {
      "NPM_ONLY": "true"
    }
  }
}
